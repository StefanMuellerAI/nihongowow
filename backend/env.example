# NihongoWOW Backend Configuration
# Copy this file to .env.local and adjust the values

# Admin emails - comma-separated list of email addresses that become admin on registration
ADMIN_EMAILS=admin@example.com,superadmin@example.com

# SMTP Settings for MFA emails (optional - if not set, codes are printed to console)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@nihongowow.com
SMTP_FROM_NAME=NihongoWOW

# OpenAI API Key (for hints feature)
OPENAI_API_KEY=sk-...

